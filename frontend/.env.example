# API 基础路径配置
# 开发环境可以设置此项，不设置则默认使用 http://localhost:8902/api
# 示例: VITE_API_BASE_URL=http://localhost:8902/api

# 注意：
# 1. 生产环境不需要设置此项，会自动使用相对路径 /api
# 2. 如果后端和前端部署在不同域名，生产环境也需要设置
# 3. 变量名必须以 VITE_ 开头才能被 Vite 识别
